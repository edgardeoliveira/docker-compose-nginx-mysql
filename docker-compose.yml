services:
  nginx:
    image: nginx
    container_name: Ed_Nginx
    ports:
      - "80:80"
  mysql:
    image: mysql:latest
    container_name: Ed_MySQL
    environment:
      - MYSQL_ROOT_PASSWORD=123456
